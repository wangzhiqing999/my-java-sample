# 2026-08-24 工作日志

## Q-P2-2 Record 简化（✅ 完成）

### 改动
- `CommonResult` / `VersionResponse` → Java 21 `record`
  - `CommonResult(int code, String msg, String data)` + 保留双参便捷构造器 `(int, String)`；删除无参构造器与 `@Data`
  - `VersionResponse(String version, String projectName)`，删除 `@Data`
  - 类级 Javadoc 用 `@param` 标注组件含义（M-P1-5 规范延续）
- `TestServiceImpl.testConfig()`：StringBuilder 拼接 → Text Block + `String.formatted()`
- 调用点适配：
  - `TestController.saveConfig`：`new CommonResult() + setCode/setMsg` → `new CommonResult(request.getCode(), request.getMsg())`
  - 测试访问器 `getCode()/getMsg()/getData()/getVersion()/getProjectName()` → `code()/msg()/data()/version()/projectName()`
  - `TestServiceImplCoreTest.testSaveConfig`：无参+setter → 双参构造

### 关键验证
- **Jackson 3.2.0 原生支持 Record**：序列化（`/version` 返回 `{version,projectName}`）与反序列化（`testLoadConfig` 用规范构造器读 `{code,msg,data}`）均通过——API 契约不变
- JUnit 49/49 通过（14 containers）
- javac 编译 0 errors（仅既有 deprecation）
- Javadoc 静态校验 62 个 public 方法零遗漏（脚本加 `public record` 声明行排除，Record 自动访问器不在源码检查范围）

### 变更统计
- 7 文件 +45/-101；main：CommonResult/VersionResponse/TestController/TestServiceImpl；test：TestControllerTest/TestServiceImplCoreTest/TestServiceImplTest

### 待办
- **P1 已全部清零**；Q-P2 系列：Q-P2-1/2/3/4/5 完成；**PERF-P2-1/2 完成**
- P2 剩余：DOC-P2-1/2/3
- 未评估：FN-P0-1/2/3

## PERF-P2-1/2 性能优化（✅ 完成）

### PERF-P2-1：BouncyCastle Provider 改为 @Bean 注册
- 新增 `config/SecurityConfig.java`（`@Configuration` + `@Bean` 注册 `BouncyCastleProvider`，Spring 单例语义保证只注册一次）
- 移除 `ECCCrypto` 与 `ECCKeyReader` 中的 `static` 块
- 两类各新增 `private static void ensureProvider()` 惰性兜底：非 Spring 环境（单元测试直接调用）首次调用时自动注册，Spring 环境下 `getProvider("BC") != null` 直接返回（no-op）
- `encrypt()`/`decrypt()` 及 `ECCKeyReader` 5 个公开方法入口均加 `ensureProvider()` 调用
- JUnit 49/49 通过：`TestServiceImplTest.encrypt_decrypt_往返还原原文` 验证非 Spring 兜底生效

### PERF-P2-2：test() 中 JSON 解析提取为独立方法
- `TestServiceImpl.test()` 中 for 循环内 20 行 JSON 解析逻辑 → 提取为 `private void parseAndLogJsonResult(Map<String, Object>)`
- `test()` 方法仅保留调用链路编排，职责更清晰
- Javadoc 补全

### 变更统计
- 5 文件（新增 SecurityConfig.java，修改 ECCCrypto.java/ECCKeyReader.java/TestServiceImpl.java + 审查清单）
- 源文件 29 → 30；javac 30 文件 0 errors；JUnit 49/49

## DOC-P2-1 README 接口文档同步（✅ 完成）

### 核对结果（TestController 13 个端点 vs README）
- **发现遗漏**：`GET /test/config`（测试默认配置）未在 README 接口文档列出
- **发现过时示例**：定时任务章节 `log.info("定时任务调用结果：" + result)` 仍是字符串拼接（实际代码 Q-P1-3 已改占位符）
- **发现结构缺失**：项目结构树缺 `exception/GlobalExceptionHandler.java`、`config/SecurityConfig.java`（PERF-P2-1 新增）

### 改动（readme.md 一处文档）
1. 「配置存取」章节补齐 `GET /test/config` 表格行 + curl 示例；`/test/read-config` 说明补充 data 内容
2. 「功能特性」表格配置管理行补充 `/test/config` 验证说明
3. 定时任务章节示例代码改占位符 `{}`（加注释"使用占位符，避免字符串拼接"）
4. 项目结构树：config 下补 `SecurityConfig.java`、新增 `exception/` 目录节点，util 树 `PemUtils.java` 修正为 `└──` 闭合

### 验证
- 纯文档修改，无代码/测试变更；13 端点与 README 表格现已一一对应
- P2 剩余：DOC-P2-2/3；未评估：FN-P0-1/2/3

## DOC-P2-2 SQL 脚本文档单一来源化（✅ 完成）

### 背景
- 项目无独立 `.sql` 文件，数据库脚本以「README 内嵌 8 段」形式存在，无单一事实来源，变更易漏同步

### 改动（新增 2 文件 + readme.md 重构）
1. **新增 `sql/init.sql`**：README 内嵌脚本提取并收敛——`test_log`/`common_config` 表（`CREATE TABLE IF NOT EXISTS`）+ 8 个函数（`CREATE OR REPLACE`），`BEGIN/COMMIT` 事务包裹，幂等可重复执行；每个函数头部注释标注 Mapper 调用点；头部注释写明用途/执行方式/维护规范
2. **新增 `sql/README.md`**：脚本清单、执行方式（`psql -f sql/init.sql`）、**数据库变更三步工作流**（改 Mapper → 改 init.sql → 更新 README 对象清单）、命名约定（小写蛇形、`p_` 前缀参数）
3. **readme.md**：「数据库初始化」章节由内嵌 8 段完整脚本重构为对象清单表（10 行：2 表 + 8 函数，含 Mapper 调用点列）+ 执行方式 + 变更规范；安装步骤改为 `psql -f sql/init.sql`；贡献指南「同步更新 README 内 SQL」→「同步更新 sql/init.sql」；项目结构树补 `sql/` 节点

### 验证
- 纯文档/脚本变更（无 .java 改动），无需编译；Mapper 中 8 个数据库函数与 init.sql 逐项核对一一对应
- P2 剩余：**DOC-P2-3**（Swagger/OpenAPI）；未评估：FN-P0-1/2/3

## DOC-P2-3 Swagger/OpenAPI 接口文档（✅ 完成）

### 方案调研
- springdoc-openapi **3.x 为 Spring Boot 4 兼容线**（Jackson 3 迁移完成，官方提供 Spring Boot 4 WebMVC demo）；2.x 仅支持 Spring Boot 3
- 选用 **3.0.3**（2026-04 发布，123 usages；3.1.0 太新仅 3 周）；swagger-api 依赖版本 **2.2.47**（父 POM 属性 `swagger-api.version`）
- **Spring Boot 4 必须显式开启**：`springdoc.api-docs.enabled=true` + `springdoc.swagger-ui.enabled=true`

### 改动（2 新增 + 5 修改）
1. **pom.xml**：新增 `springdoc-openapi-starter-webmvc-ui` 3.0.3
2. **application.yml**：`springdoc.api-docs.enabled/path` + `springdoc.swagger-ui.enabled/path`
3. **新增 `config/OpenApiConfig.java`**：`@Configuration` + `@OpenAPIDefinition`（title=test-service API, version=1.1-SNAPSHOT, description）+ `@Bean GroupedOpenApi`（分组 `public-api`，匹配 `/test/**`）
4. **TestController**：类级 `@Tag` + 13 个端点 `@Operation(summary)`（中文摘要）
5. **3 个模型类** `@Schema`：`CommonResult`（code/msg/data 字段描述+example）、`VersionResponse`、`SaveConfigRequest`
6. **readme.md**：「使用示例」章节加 Swagger 使用说明；功能特性/技术栈表格补「接口文档」行

### 关键验证
- 手动下载 6 个 jar 到 target/deps：starter-common/webmvc-api/webmvc-ui 3.0.3 + swagger-annotations/models/core-jakarta 2.2.47
- javac 31 源文件（含 OpenApiConfig）编译 0 errors；JUnit 49/49 通过
- 字节码验证：TestController 16 处 swagger/Operation/Tag 引用、OpenApiConfig 27 处、CommonResult 7 处 Schema
- 文档地址：`/swagger-ui.html`（UI）、`/v3/api-docs`（JSON）、`/v3/api-docs.yaml`

### 变更统计
- 7 文件（新增 OpenApiConfig.java，修改 pom/application.yml/TestController/CommonResult/VersionResponse/SaveConfigRequest + 审查清单 + readme）
- 源文件 30 → 31；**P2 全部完成**（Q-P2-1~5、PERF-P2-1/2、DOC-P2-1/2/3）

### 待办
- **P2 已全部清零**（Q-P2 系列 + PERF-P2 + DOC-P2 系列）
- 未评估：**FN-P0-1/2/3**（核心路径异常处理/事务边界/资源关闭）

## FN-P0-1 核心业务路径异常处理（✅ 完成）

### 审查结论
- **唯一 P0 缺陷**：`TestServiceImpl.testSaveConfig()` 吞掉数据库写入异常——`catch (Exception ex) { log.error(...) }` 后静默返回，Controller 仍回 200 success，客户端误判保存成功（违反 E-06 规范）
- 其余核心路径已合规：加解密 `throws Exception`、健康检查 try-catch 返 DOWN、MANIFEST 读取 catch 返默认值、Client 实现纯内存、定时任务无 IO 路径

### 改动（4 main + 2 test）
1. **`TestService.testSaveConfig`**：签名加 `throws Exception` + Javadoc `@throws`
2. **`TestServiceImpl.testSaveConfig`**：移除 try-catch，异常上抛（与 `testLoadConfig`/`saveLogData`/`encrypt`/`decrypt` 模式一致）
3. **`TestController.saveConfig`**：加 `throws Exception` + Javadoc `@throws`（异常穿透至 `GlobalExceptionHandler` → 500）
4. **`test()` 接口+实现**：Javadoc 补 `@throws RuntimeException` 说明（8 个数据库调用异常上抛，由 `GlobalExceptionHandler` 兜底，MyBatis 运行时异常无需 checked 声明）
5. **回归测试 +2 用例**（49 → 51）：
   - `TestServiceImplCoreTest.testSaveConfig_数据库写入失败时异常上抛不吞异常`（Mapper 桩 `fn_save_config` 抛异常 → assertThrows）
   - `TestControllerTest.saveConfig_service抛异常时Controller不吞异常直接上抛`（Service 桩 `testSaveConfig` 抛异常 → assertThrows）
   - 现有 2 个用例方法签名适配 `throws Exception`

### 关键验证
- javac 31 源文件 0 errors；**JUnit 51/51 通过**（14 containers）

### 变更统计
- 6 文件（TestService/TestServiceImpl/TestController + CoreTest/ControllerTest + 审查清单）
- FN-P0-1 完成；**剩余未评估：FN-P0-2（事务边界）/FN-P0-3（资源关闭）**

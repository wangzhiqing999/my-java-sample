# 2026-08-19 工作日志

## 上传 README 到 WorkBuddy 资料库

- 将项目 `readme.md`（test-service 项目 README，约 20KB）上传至 WorkBuddy 原生资料库
- 使用「资料库」skill 的 doc 创建流程：读取运行环境 → 客户端鉴权 → dry-run 校验 → create_doc.py 正式创建
- 目标为「我的文档」（未显式指定空间，默认 personal 分类，无需团队确认）
- 创建结果：kind=doc，节点 ID `cusPpbT1DJaUEMYCHhwtYE`，链接 https://www.workbuddy.cn/space/d/cusPpbT1DJaUEMYCHhwtYE，failedCount=0 / fatalCount=0（内容完整）

## 制定代码规范与 Review 检查清单 + 修复 P0-1

### 1. 产出 CODE_STANDARDS_AND_REVIEW_CHECKLIST.md

- 基于全项目源码逐行分析，产出 12 类代码规范 + P0/P1/P2 三级 Review 检查清单
- 每项检查关联当前项目实际违规位置，末尾附改进项汇总表（P0×5 / P1×9 / P2×5）

### 2. 修复 P0-1：AesEcbUtils 硬编码密钥 + ECB 模式

- 删除 `util/AesEcbUtils.java`（硬编码密钥 `"it_is_a_test_pwd"` + AES/ECB/PKCS5Padding 不安全模式）
- 新增 `util/AesGcmUtils.java`：AES/GCM/NoPadding 认证加密，每次加密随机 12 字节 IV，密钥由调用方传入（16/24/32 字节校验），返回 Base64(IV+密文)，final 类 + 私有构造
- 运行时验证 6 项全部通过：对称性 / 随机IV / 错误密钥拒绝(AEADBadTag) / 篡改检测(AEADBadTag) / 密钥长度校验 / null 校验
- 同步更新：readme.md 文件树（AesEcbUtils→AesGcmUtils）、检查清单文档 P0 项标记 ✅ 已修复
- 注意：本机无 mvn/JDK21（仅 JDK17），编译验证用 javac -encoding UTF-8 完成

### 3. 修复 P0-2（SEC-P0-3）：AesUtil 硬编码静态 IV

- 重写 `util/AesUtil.java`：AES/CBC/PKCS5Padding 保留，但每次加密生成 16 字节随机 IV，返回 `Base64(IV+密文)`，解密自动提取 IV；密钥由调用方传入并校验 16/24/32 字节；final 类 + 私有构造；移除 main 测试代码
- 运行时验证 12 项全部通过：对称性(中文/JSON) / 随机IV / 错误密钥(长度+同长度) / 篡改检测 / 128/192bit 密钥 / 非法长度拒绝 / null 防护 / 旧格式密文拒绝
- 兼容性注意：旧版固定 IV 生成的密文（无 IV 前缀）无法解密，需重新加密
- 同步更新：检查清单（SEC-P0-3 ✅、待修正项表、汇总表第 2 项、Q-P2-1 影响文件、测试清单 3b）、readme.md 文件树注释
- P0 剩余：SEC-P0-4（TestController 异常信息返回客户端）、SEC-P0-5（TestServiceImpl testLoadConfig 返回 null、health 暴露堆栈）

### 4. 修复 P0-3/P0-4（SEC-P0-4）：异常信息直接返回客户端

- 新增 `exception/GlobalExceptionHandler.java`（`@RestControllerAdvice`）：`IllegalArgumentException` → 400 + 通用参数提示；其余异常 → 500 + "服务器内部错误"；完整堆栈仅 log.error 服务端记录
- `TestController.encrypt/decrypt/saveLogData` 移除 `return e.getMessage()`，异常上抛由全局处理器接管；`saveLogData` 方法签名加 `throws Exception`（接口本就声明）；顺手清除 encrypt/decrypt 中 3 处 `System.out.println` → `log.debug`
- 编译验证：**全量 29 个源文件 javac 编译通过**（exit 0）。关键坑：本机 m2 缺 bouncycastle 1.78.1 / tools.jackson jackson-core+databind 3.2.0 / java-jwt 4.4.0，需 curl 从 Maven Central 下载；且 Git Bash 的 POSIX 路径（/d/...）Windows javac 不识别，必须用 D:/ 风格路径拼 classpath
- 运行时验证 6 项通过：500 状态码 / 通用 body / 不泄露 IP·SQL·AES 细节 / 400 参数异常 / 通用参数提示
- 同步更新检查清单：SEC-P0-4 ✅、汇总表第 3 项 ✅、第 7 项（println）✅、第 14 项（全局异常处理）✅
- P0 剩余（汇总表 4/5）：EXC-P0-1（testLoadConfig 吞异常返回 null）、EXC-P0-2（health 暴露 stackTrace）

### 5. 修复 EXC-P0-1：testLoadConfig 吞异常返回 null

- `TestServiceImpl.testLoadConfig()` 移除 try-catch 的 `return null`，异常直接上抛；接口 `TestService` 与 `TestController.readConfig` 同步声明 `throws Exception`（由已有 GlobalExceptionHandler 统一兜底）；顺手修正日志文案复制粘贴错误（"保存"→删除，异常不再本地记录）
- 编译验证：全量 29 源文件 javac 通过（exit 0）
- 运行时验证 6 项通过（JDK 动态代理 Mock TestMapper）：正常路径返回非 null 且 msg 正确 / mapper 异常原样上抛（消息保留）/ 非法 JSON 时 Jackson 解析异常上抛
- **环境新坑**：Jackson 3.x（tools.jackson）运行时要求 jackson-annotations ≥ 2.21（`JsonSerializeAs` 注解 2.21 才引入，2.19.1 会 NoClassDefFoundError）；且 2.21 版本号无 .0 后缀（2.21，非 2.21.0），下载时 404 过两次
- 同步更新检查清单：EXC-P0-1 ✅、汇总表第 4 项 ✅
- P0 剩余 1 项（汇总表 5）：EXC-P0-2（health() 暴露 stackTrace）

### 6. 修复 EXC-P0-2：health() 暴露 stackTrace —— **P0 全部清零**

- `TestServiceImpl.health()` 移除 `result.put("stackTrace", sw.toString())` 与 `result.put("error", e.getMessage())`；保留 try-catch（健康检查语义：异常时返回 DOWN 而非 500），异常细节改 `log.error("健康检查失败，数据库连接异常", e)` 仅服务端记录；返回 `status=DOWN` + 通用提示"服务不可用，请稍后重试"；清理无用 import（StringWriter/PrintWriter）
- 编译验证：全量 29 源文件 javac 通过（exit 0）
- 运行时验证 7 项通过：正常路径 UP 且无 stackTrace / 异常路径 DOWN 且无 stackTrace·无 error 字段 / message 为通用描述 / 响应整体不泄露 SQLException·IP·连接细节
- 同步更新检查清单：EXC-P0-2 ✅、汇总表第 5 项 ✅
- **P0 全部 5 项已清零**。遗留观察：`AppleClientSecret.java:85` 仍有 `e.printStackTrace()`（类似问题，P1 级别，建议后续处理）

## 下一步候选（P1）
- 字段注入 → 构造器注入（@RequiredArgsConstructor）
- AppleClientSecret e.printStackTrace / DES 使用
- System.out.println（TestServiceImpl.test 中）/ 日志字符串拼接（log.debug("解密后: " + decryptedText)）
- 缺少 @Override 注解（ClientAServiceImpl/ClientEmptyServiceImpl 等）
- 方法命名 test_sum_array（PostgreSQL 侧存储过程名一致，需谨慎）
- 无测试代码、无统一 response 封装（返回裸 String）

### 7. 修复 P1-1：字段注入 → 构造器注入

- 3 文件 7 处 `@Autowired` 字段注入全部改为 Lombok `@RequiredArgsConstructor` + `private final` 字段：
  - `TestController.java`（4 处：testService/configData/clientService/otherClientService）
  - `TestServiceImpl.java`（2 处：testMapper/configData）
  - `TestScheduledTask.java`（1 处：testService）
- 编译验证：全量 29 源文件 javac 通过（exit 0）
- 运行时验证 14 项通过（反射检查构造器生成 + 构造器实例化可用性）
- 同步更新检查清单：Q-P1-1 ✅、汇总表第 6 项 ✅
- 下一步候选（P1）：日志字符串拼接（`{}` 占位符）、AppleClientSecret 的 e.printStackTrace / DES、缺少 @Override、方法命名 test_sum_array、无测试代码

### 8. 修复 P1-2（Q-P1-3）：日志字符串拼接 → `{}` 占位符

- 2 文件 2 处拼接全部改为占位符：`TestServiceImpl.java:135`（`log.debug("解密后: " + x)` → `log.debug("解密后: {}", x)`）、`TestScheduledTask.java:33`（`log.info("定时任务调用结果：" + r)` → `log.info("定时任务调用结果: {}", r)`）
- 全项目 grep 确认 slf4j 日志已无任何 `+` 拼接
- 编译验证：全量 29 源文件 javac 通过（exit 0）
- 同步更新检查清单：Q-P1-3 ✅、汇总表第 8 项 ✅、日志规范反例示例加"已修复"状态说明
- 下一步候选（P1）：Q-P1-2 System.out.println（工具类 main 方法中多处，与 Q-P2-1 迁移测试重叠）、AppleClientSecret e.printStackTrace / DES、Q-P1-4 缺失 @Override、Q-P1-5 方法命名 test_sum_array、无测试代码

### 9. 修复 P1（Q-P1-4）：缺失 @Override 注解

- 全项目扫描 7 个接口实现类，补齐 6 处缺失 `@Override`：
  - `TestServiceImpl.java` 5 处：`saveLogData` / `dailyTask` / `testConfig` / `testSaveConfig` / `testLoadConfig`
  - `ClientBServiceImpl.java` 1 处：`getClientInfo()`（此前该实现类仅 getTodoList 有注解）
- 其余 5 个实现类（ClientA/ClientEmpty/OtherClientC/D/Empty）原本已全部标注
- 编译验证：全量 29 源文件 javac 通过（exit 0，仅原有 deprecation/unchecked 提示）
- 运行时验证：反射比对 7 个实现类 × 接口全部 19 个方法签名，ALL IMPLEMENTED
- 同步更新检查清单：Q-P1-4 ✅、汇总表第 9 项 ✅
- 下一步候选（P1）：Q-P1-2 System.out.println、AppleClientSecret e.printStackTrace / DES、Q-P1-5 方法命名 test_sum_array、Q-P1-6 具体 HTTP 方法注解、无测试代码

### 10. 修复 P1（Q-P1-5）：方法命名 test_sum_array → testSumArray

- `TestMapper.test_sum_array(int[])` → `testSumArray(int[])`，调用点 `TestServiceImpl.java:98` 同步、日志文案同步
- **关键**：`@Select("SELECT test_sum_array(#{datas})")` 中的 `test_sum_array` 是 PostgreSQL 数据库函数名，**保留不动**——只重命名 Java 方法标识符，不影响存储过程调用
- 编译验证：全量 29 源文件 javac 通过（exit 0）
- 运行时验证 4 项通过（反射）：新方法 `testSumArray(int[])` 存在返回 int / `@Select` SQL 仍为 `SELECT test_sum_array(#{datas})` / PG 函数名保留 / 旧方法已移除
- 源码残留确认：`test_sum_array` 仅存于 Javadoc 注释与 SQL 注解（均为数据库函数名引用）
- 同步更新检查清单：Q-P1-5 ✅、汇总表第 10 项 ✅
- 下一步候选（P1）：Q-P1-2 System.out.println、AppleClientSecret e.printStackTrace / DES、Q-P1-6 具体 HTTP 方法注解、无测试代码、统一 response 封装（M-P1-2）

### 11. 修复 P1（Q-P1-6）：具体 HTTP 方法注解

- `TestController` 3 处通用 `@RequestMapping` 全部替换：`/get`→`@GetMapping`、`/readconfig`→`@GetMapping`、`/saveconfig`→`@PostMapping`（**写操作按语义改 POST**，query 参数仍兼容）；类级 `@RequestMapping("/test")` 保留（路由前缀）
- readme 同步：`/test/saveconfig` 调用记录 GET→POST，curl 示例加 `-X POST`
- 编译验证：全量 29 源文件 javac 通过（exit 0）
- 运行时验证 5 项通过（反射）：类级注解保留 / get→GetMapping("/get") / saveConfig→PostMapping("/saveconfig") / readConfig→GetMapping("/readconfig") / 方法级通用注解零残留
- 坑：`@GetMapping` 注解实例是 JDK 代理，不能 cast 成 `@RequestMapping`，需通过注解自身的 `value()` 反射读取
- 同步更新检查清单：Q-P1-6 ✅、汇总表第 11 项 ✅
- 下一步候选（P1）：Q-P1-2 System.out.println、AppleClientSecret e.printStackTrace / DES、无测试代码、M-P1-2 统一 response 封装

### 12. 修复 P1（Q-P1-2）：System.out.println 全清除（19 处）

- **业务代码**：`Application.main()` println → `log.info("start at {}")`（SLF4J，类上加 Logger）；`PemUtils` 4 处 catch 块 println → 抛 `IllegalArgumentException`（携带 algorithm 名，**顺带消除静默返回 null**，getPublicKey/getPrivateKey 改为直接 return）
- **工具类 main 自测代码**（与 Q-P2-1 迁移测试重叠）：删除 6 个工具类 main——`DesEncryptor` / `AppleClientSecret` / `Sha256Util` / `Md5Util` / `HmacSha256Util` / `RsaUtil`，共消除 13 处 println + `AppleClientSecret` 的 `e.printStackTrace()`（遗留观察项一并清零）
- 删除后确认：工具类均未被生产代码调用（AppleClientSecret→PemUtils 是唯一生产引用链）；`Application.main` 保留（Spring Boot 入口）
- 编译验证：全量 29 源文件 javac 通过（exit 0，仅原有 deprecation/unchecked）
- 运行时验证 12 项通过：6 工具类 main 已移除 / Application.main 保留 / SHA256(123456) 与 MD5(hello world) 哈希值与标准参考值一致 / HMAC-SHA256 64 位 hex / DES 加密匹配 pgcrypto 参考值（大写 hex 因 withUpperCase）/ PemUtils 非法算法名抛 IllegalArgumentException
- 全项目 grep 确认：`System.out.println` 与 `printStackTrace` 零残留
- 同步更新检查清单：Q-P1-2 ✅、汇总表第 7 项 ✅（标注全项目 0 残留）、P2 第 15 项 🟡（main 已删，JUnit 迁移待补，需先加 test 依赖）
- 下一步候选（P1）：P1-12 DesEncryptor 用 DES（弱算法+硬编码默认密钥，与 P0-1 同类）；无测试代码（Q-P2-1，加 spring-boot-starter-test + 迁移 6 工具类验证为 JUnit）；M-P1-2 统一 response 封装

### 13. 修复 P1（SEC-P1-1）：DesEncryptor 整类删除

- **决策**：DES 无法"改用 AES"保留用途——该类专为兼容 pgcrypto `des-cbc/pad:pkcs` 而设计。grep 确认**零生产调用点**，故采用与 P0-1（AesEcbUtils）同款处理：整类删除，弱算法 + 硬编码默认密钥 `it_is_a_test_password` 一并消除
- **兼容性说明**：pgcrypto DES 加解密改用 SQL 侧 `encrypt()/decrypt()` 直接实现（原参考 SQL 在类注释中，已随类删除；如需可查 git 历史）
- 同步 readme：文件树删除 `DesEncryptor.java` 行，顺带修正重复的 `JsonUtil.java` 条目
- 编译验证：全量 **28** 源文件 javac 通过（exit 0，仅原有 deprecation/unchecked）
- 运行时验证 6 项通过：`Class.forName("...DesEncryptor")` 抛 ClassNotFoundException / AesGcmUtils Base64(IV+cipher) 格式 / roundtrip 解密还原 / 随机 IV 两次密文不同 / AesUtil(AES-CBC) roundtrip / 错误密钥被 GCM 认证拒绝
- 全项目 grep 确认：`DesEncryptor`、`Cipher.getInstance("DES`、`"DES/` 零残留
- 同步更新检查清单：SEC-P1-1 ✅、待修正项表第 233 行 ✅、汇总表第 12 项 ✅；readme 文件树同步
- 下一步候选（P1）：无测试代码（Q-P2-1，加 spring-boot-starter-test + 迁移工具类验证为 JUnit）；M-P1-2 统一 response 封装；SEC-P1-2 MD5 不用于密码哈希（Md5Util 实际仅做摘要，非密码场景，待评估）

### 14. 修复 P2（Q-P2-1）：无测试代码 → JUnit 迁移

- **pom.xml**：新增 `spring-boot-starter-test`（scope=test，版本由 spring-boot-starter-parent 4.1.0 管理）
- **新增 7 个测试类**（`src/test/java/com/my/work/util/`，JUnit 5 Jupiter，共 19 用例）：
  - `AesGcmUtilsTest`（7）：16/24/32 字节密钥 roundtrip、Base64(IV+密文) 格式、随机 IV 两次密文不同、错误密钥拒绝（GCM 认证）、篡改密文拒绝
  - `AesUtilTest`（3）：AES-CBC roundtrip（含中文）、随机 IV、16 字节 IV 前缀
  - `Sha256UtilTest`（2）：SHA-256("123456") / SHA-256("") 标准参考值
  - `Md5UtilTest`（1）：MD5("hello world") 标准参考值（注明禁止用于密码哈希）
  - `HmacSha256UtilTest`（2）：RFC 4231 Test Case 2 参考值、64 位 hex 格式
  - `RsaUtilTest`（2）：RSA-2048 roundtrip、错误私钥解密失败
  - `PemUtilsTest`（2）：PKCS8 PEM 私钥解析、非法算法名抛 IllegalArgumentException
- **本地验证（无 mvn）**：下载 `junit-platform-console-standalone-1.11.4.jar` 到 target/deps，javac 编译 main(28)+test(7) 后 `java -jar console-standalone execute --scan-class-path "target/javac-check;target/test-check"` → **19/19 通过**
- **坑**：`--scan-class-path` 无参数会扫描整个 classpath（含 m2 几百个 jar），产生 1 个"幽灵失败"（jdt ClassNotFound 类加载副作用）——**必须显式指定扫描目录** `target/javac-check;target/test-check`
- 同步更新检查清单：Q-P2-1 ✅、汇总表第 13 项 ✅、P2 第 15 项 ✅；readme 文件树补 `src/test` 行
- 下一步候选（P1）：M-P1-2 统一 response 封装；SEC-P1-2 MD5 密码哈希评估（Md5Util 仅做摘要，非密码场景）；Q-P2-2 Java 21 特性简化（Record/Pattern Matching）；Q-P2-3 移除 JsonUtil 注释掉的 import



## Q-P2-3 移除 JsonUtil 注释掉的 import
- 删除 `JsonUtil.java` 中 `// import tools.jackson.datatype.jsr310.JavaTimeModule;`（Jackson 3.x 已废弃，仅作对比）；顺带清理 package 后多余空行、`java.util` import 字母序（Collections 在 Map 前）
- grep 确认全项目注释 import 零残留；28 源文件 javac 编译通过（仅既有 deprecation/unchecked 提示）
- 同步检查清单：Q-P2-3 ✅（含说明）、汇总表第 17 项 ✅；readme 仅文件树引用 JsonUtil，无需改动
- 下一项候选：Q-P2-4 Controller 内部类 VersionResponse 提取独立文件

## Q-P2-4 Controller 内部类 VersionResponse 提取独立文件
- 新建 `com.my.work.model.VersionResponse`（与 CommonResult 同包，@Data + 全参构造，含 Javadoc）
- `TestController`：删除内部类 `VersionResponse`，改 import `com.my.work.model.VersionResponse`，顺带移除不再使用的 `lombok.Data` import
- grep 确认全项目无残留 `static class`；29 源文件 javac 编译通过（0 errors）
- 同步检查清单：Q-P2-4 ✅（含说明）、汇总表第 16 项 ✅；readme 文件树 model 包补 VersionResponse 行
- 下一项候选：Q-P2-2 Java 21 特性简化（Record/Pattern Matching）或 Q-P2-5 @RequestMapping 路径风格统一

## Q-P2-5 @RequestMapping 路径风格统一
- 统一为 kebab-case（多词路径短横线分隔）：`/saveconfig`→`/save-config`、`/readconfig`→`/read-config`、`/savelog`→`/save-log`、`/otherinfo`→`/other-info`；单字路径与 `/daily-task` 已合规不变
- TestController：4 处注解 + Javadoc 2 处 + log.debug 2 处同步；readme：接口表格 4 处 + curl 3 处 + 时序图 1 处同步
- grep 确认 src/readme 旧路径零残留（检查清单历史记录除外）；29 源文件 javac 编译通过（0 errors）
- 同步检查清单：Q-P2-5 ✅（含说明）；汇总表无该项独立条目
- 注意：接口路径变更属 breaking change，.NET 测试客户端（TestCallAll/TestAppCall）如调用这些接口需同步更新
- 下一项候选：Q-P2-2 Java 21 特性简化（Record 替代 CommonResult/VersionResponse）

## 修复 SEC-P0-1 漏检项：AppleClientSecret 硬编码 EC 私钥
- **触发**：Code Review 复检时发现 `AppleClientSecret.java:24-28` 含硬编码 EC P-256 私钥常量 `PRIVATE_KEY_256`，原 SEC-P0-1 漏检（此前仅检查 AesEcbUtils/AesUtil）
- **核实**：
  - `createClientSecret(String privateKeyString)` 方法已通过参数接收私钥 → 类内 `PRIVATE_KEY_256` 常量为死代码
  - `client_id`/`team_id`/`key_id` 均为 `"xxx"` 占位符
  - 全项目 grep 确认：无任何文件 `import com.my.work.util.AppleClientSecret`，无任何文件 `import com.auth0.jwt`（即 java-jwt 4.4.0 在 pom 中已成无用依赖）
- **决策**：整类删除（与 `DesEncryptor` 同款处理：无调用点 + 硬编码密钥 + 占位符字段，git 历史可查）
- **同步更新**：
  - `readme.md` 文件树移除 `AppleClientSecret.java` 行
  - 检查清单 SEC-P0-1：检查范围补"含工具类、配置类、JWT/签名类等"，特别关注补 `AppleClientSecret.PRIVATE_KEY_256`，当前状态补"删除 AppleClientSecret.java"
  - 检查清单 P0 汇总表新增第 6 项（`AppleClientSecret.java`）
  - 检查清单 Q-P1-2 补"后续 AppleClientSecret 整类删除（SEC-P0-1 漏检项）"
- **编译验证**：本机 m2 缺 spring-boot 4.1.0 / bouncycastle 1.78.1 / jackson 3.2.0 / java-jwt 4.4.0，mvn 被沙箱拦截，未做完整 javac 验证；但 grep 双重确认无引用 + 工作日志此前已验证 29 源文件编译通过，删除一个无引用的类不会引入新编译错误
- **遗留**：pom.xml 中 `java-jwt` 4.4.0 依赖变成无用依赖，可在后续清理 pom 时移除

## 修复 M-P1-3：工具类 final + 私有构造
- **范围**：`com.my.work.util` 包下 8 个工具类，其中 `AesGcmUtils`/`AesUtil` 此前已修，本次补齐剩余 6 个
- **修改**（6 文件 6 处 `public class` → `public final class` + 新增私有构造方法）：
  - `Md5Util`：`public final class` + `private Md5Util() {}`
  - `Sha256Util`：同上
  - `HmacSha256Util`：同上
  - `RsaUtil`：同上
  - `PemUtils`：同上
  - `JsonUtil`：同上
- grep 双重验证：
  - 8 个工具类全部为 `public final class`（`AesGcmUtils`/`AesUtil`/`JsonUtil`/`Sha256Util`/`HmacSha256Util`/`Md5Util`/`PemUtils`/`RsaUtil`）
  - 8 个工具类全部有私有构造方法
- **编译验证**：本机 m2 缺 spring-boot 4.1.0 / bouncycastle 1.78.1 / jackson 3.2.0 / java-jwt 4.4.0，mvn 被沙箱拦截；但修改仅是类声明加 `final` + 新增私有构造方法，纯结构性修改不影响静态调用，工作日志此前已验证 29 源文件 javac 编译通过
- **同步更新**：
  - 检查清单 M-P1-3：`[ ]` → `[x] ✅`，说明补"8 个工具类全部 final + 私有构造"
  - 检查清单 P1 汇总表新增第 15 项（工具类 final）
  - 检查清单 P2 汇总表第 18 项：标 ✅ 已修复
- 下一项候选（P1 剩余）：M-P1-1 Controller 业务逻辑下沉 Service（encrypt/decrypt）、M-P1-2 统一 CommonResult 响应格式、SEC-P1-3 输入参数校验（@Valid + DTO）

## 修复 M-P1-2：统一 CommonResult 响应格式
- **范围**：`TestController` 13 个接口 + `GlobalExceptionHandler` 同步统一
- **决策**（用户确认）：
  - 全部 CommonResult 包装（业务数据放 data 字段，对象/Map 用 `JsonUtil.toJson` 序列化）
  - Service 层保持原签名（如 `String dailyTask()`），Controller 层做转换，影响最小
- **修改**：
  - `TestController`：13 个接口返回类型全部改为 `CommonResult`，新增 3 个 `success(...)` 辅助方法（无数据/字符串/对象）
  - `GlobalExceptionHandler`：返回类型从 `ResponseEntity<String>` 改为 `ResponseEntity<CommonResult>`，与正常响应格式一致
- grep 验证：13 个接口均为 `public CommonResult`
- **编译验证**（本次突破性进展）：
  - 本机 m2 缺 spring-boot 4.1.0/bouncycastle 1.78.1/jackson 3.2.0/java-jwt 4.4.0/mybatis 等
  - 从 Maven Central 下载到 `target/deps/`：jackson-core-3.2.0、jackson-databind-3.2.0、jackson-annotations-2.21、bcprov-jdk18on-1.78.1、bcpkix-jdk18on-1.78.1、mybatis-3.5.16、spring-boot-autoconfigure-3.5.3、lombok-1.18.34
  - 发现 Lombok 1.18.8 与 JDK 21 不兼容（`IllegalAccessError: class lombok.javac.apt.LombokProcessor cannot access class com.sun.tools.javac.processing.JavacProcessingEnvironment`），需 1.18.30+ 才支持 JDK 21
  - 最终用 JDK 21 + Lombok 1.18.34 + 完整 classpath，**28 个源文件 javac 编译 EXIT: 0**，只有一个已弃用 API 警告（`TestServiceImpl` 使用了压缩时间的 API，与本次修改无关）
  - 编译后清理 `target/javac-check` 和 `target/deps`
- **同步更新**：
  - `readme.md`：使用示例章节添加响应格式说明，更新基础测试表格说明
  - 检查清单 M-P1-2：`[ ]` → `[x] ✅`，详细说明修复内容
  - 检查清单 P1 汇总表新增第 16 项（统一响应格式）
- **遗留观察**：pom.xml 中 Lombok 版本仍是 1.18.8，与项目目标 JDK 21 不兼容。后续可在清理 pom 时升级到 1.18.34+
- 下一项候选（P1 剩余）：M-P1-1 Controller 业务逻辑下沉 Service（encrypt/decrypt）、SEC-P1-3 输入参数校验（@Valid + DTO）

## 修复 pom.xml Lombok 版本不兼容
- **问题**：Spring Boot 4.1.0 BOM 默认 Lombok 1.18.8 与 JDK 21 不兼容（`IllegalAccessError: class lombok.javac.apt.LombokProcessor cannot access class com.sun.tools.javac.processing.JavacProcessingEnvironment`），1.18.30+ 才支持 JDK 21
- **修改**：`pom.xml` `properties` 节点新增 `<lombok.version>1.18.34</lombok.version>`，覆盖 BOM 管理的版本（`<dependency>` 块无需改 `<version>`，由 BOM 统一管理）
- **验证**（本次改用 mvn，告别手工 javac 拼装 classpath）：
  - `mvn -DskipTests compile` → **BUILD SUCCESS**（exit 0）
  - `mvn dependency:list` 确认实际解析版本 `org.projectlombok:lombok:jar:1.18.34:compile`，BOM 版本被正确覆盖
- **同步更新**：检查清单 P1 汇总表新增第 17 项（pom.xml Lombok 版本），状态 ✅ 已修复
- 下一项候选（P1 剩余）：M-P1-1 Controller 业务逻辑下沉 Service（encrypt/decrypt）、SEC-P1-3 输入参数校验（@Valid + DTO）

## 文档同步：检查清单勾选状态（消除失真）
- 勾选 3 项实际已完成但清单未勾选的检查项：SEC-P1-4（GlobalExceptionHandler 已存在）、TEST-P1-1（7 个 JUnit 测试类已建，19/19 通过）、M-P1-4（ConfigData 3 字段已补 Javadoc），均补充修复说明
- 清理过时条目：① 安全规范待修正表删除 `RsaUtil.java` main 占位符行（main 已随 Q-P1-2 删除）；② 命名规范 N-10 待修正项标注 Q-P1-5 ✅；③ API 规范待修正项标注 Q-P1-6 已修 + 注明"松散参数 saveConfig"仍待处理（对应 SEC-P1-3）；④ 配置管理/注释文档规范待修正项标注已修（M-P1-4/Q-P1-4 ✅）；⑤ 测试规范"无任何测试代码"改为已覆盖 2/3/3b、仍缺 TEST-P1-2/3
- 复核残留"存在违规/缺失"项（SEC-P1-2、SEC-P1-3、M-P1-1、M-P1-5、TEST-P1-2/3）均属实未完成，保留不动
- 纯文档变更，无代码改动；commit 500b8ee 之后新增改动未提交

## SEC-P1-2 MD5 不用于密码哈希或签名（评估 + 加警示）
- 评估结论：`Md5Util` 生产代码零调用点，仅 `calculateMD5` 通用摘要，未用于密码哈希/签名（无 HMAC-MD5/RSA-MD5）；MD5 作为非安全场景摘要（数据指纹/旧系统兼容）保留
- 修改：`Md5Util.java` 类级 Javadoc 加安全警示（禁止密码哈希/签名/HMAC/证书指纹，应使用 BCrypt/Argon2/PBKDF2），方法 Javadoc 同步标注；逻辑零改动
- 验证：28 main 源文件 javac 0 errors；JUnit 19/19 通过（踩坑：cp.txt 需在下载 junit jar 后重建，否则测试编译报 org.junit.jupiter.api 不存在）
- 同步检查清单：SEC-P1-2 ✅（含评估说明）、安全规范待修正表 Md5Util 行标注已评估、汇总表 P1 补编号 18
- 事实修正：main 源文件数为 28（Q-P2-4 轮日志记 29 有误，已核对文件清单）

## SEC-P1-3 接口输入参数校验（saveConfig 封装 DTO + @Valid）
- 新建 `model/SaveConfigRequest`（@Data：`code` @NotNull+@Min(0)、`msg` @NotBlank），`saveConfig(int code, String msg)` → `@Valid @ModelAttribute SaveConfigRequest`
- Controller 类级 `@Validated`；`/encrypt` `/decrypt` `/save-log` 的 `@RequestBody String` 加参数级 `@NotBlank`
- `GlobalExceptionHandler` 新增 6 类参数绑定/校验异常统一 400（MethodArgumentNotValidException/BindException/ConstraintViolationException/HttpMessageNotReadableException/MissingServletRequestParameterException/MethodArgumentTypeMismatchException）——否则 @Valid 失败会落 Exception 兜底变 500
- 依赖：`spring-boot-starter-validation` 已在 pom；m2 已有 jakarta.validation-api-3.0.2 + hibernate-validator-8.0.2，无需新下载
- 新增 `SaveConfigRequestTest` 4 用例（合法通过 + code 空/负 + msg 空白拒绝），用 `ParameterMessageInterpolator` 规避 jakarta.el 依赖
- 验证：29 main 源文件 javac 0 errors；JUnit 23/23 通过（19 原有 + 4 新增）
- 同步检查清单：SEC-P1-3 ✅（含说明）、Q-P1-6 段"松散参数待处理"注释更新、汇总表 P1 补编号 19；readme 接口表格 + 文件树补 SaveConfigRequest

## M-P1-1 Controller 业务逻辑下沉 Service（2026-08-20）
- TestService 接口新增 3 方法：getVersion() / encrypt(String) / decrypt(String)，均带完整 Javadoc
- TestServiceImpl 实现：getVersion（MANIFEST.MF 解析，读取失败 log.warn + 回退 unknown/test-service）、encrypt（读公钥 + ECCCrypto）、decrypt（读私钥 + ECCCrypto），复用已有 configData 依赖
- TestController：3 个方法改为纯 HTTP 层调用 success(testService.xxx())；移除 configData 字段 + ECCCrypto/ECCKeyReader/Manifest/Attributes/InputStream/IOException/PrivateKey/PublicKey/VersionResponse 等无用 import
- 新增 TestServiceImplTest 4 用例（JDK Proxy 桩替代 mockito——m2 无 mockito/byte-buddy；密钥硬编码自 application.yml 演示密钥）：getVersion 非空、encrypt 返回 Base64、encrypt→decrypt roundtrip、篡改密文被 GCM 认证拒绝
- 验证：29 main 源文件 javac 0 errors；JUnit 27/27 通过（23 原有 + 4 新增）
- 同步检查清单：M-P1-1 ✅（含说明）、汇总表 P1 补编号 20；readme 无需改动（描述均为 HTTP 层行为）
- 下一项候选：M-P1-5 public 方法 Javadoc 完整性，或 TEST-P1-2/3 Service/Controller 集成测试
